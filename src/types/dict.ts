export interface DictItem {
  createBy: string;
  createTime: string;
  updateBy?: string;
  updateTime?: string;
  dictCode: number;
  dictSort: number;
  dictLabel: string;
  dictValue: string;
  dictType: string;
  status: string;
  code: string;
}